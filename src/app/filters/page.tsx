'use client';

import { UserFilters } from "@/components/user-filters/UserFilters";

export default function FiltersPage() {
    return (
        <div>
            <UserFilters />
        </div>
    )
}